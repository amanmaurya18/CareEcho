# ------------------------------------------------------------------
# CareEcho environment (ALL OPTIONAL — the app runs in Demo Mode
# without any of these and never crashes)
# ------------------------------------------------------------------

# Google Gemini API key → enables REAL prescription OCR (/api/scan-prescription)
# and AI voice-companion replies (/api/voice-companion).
# Get one free at https://aistudio.google.com/app/apikey
GEMINI_API_KEY="https://www.instagram.com/reel/Ddb8wQETVR9/?stkn=NTc4MTIwNjQ2YQ=="

# Twilio (optional) → real WhatsApp/SMS dispatch in /api/send-alert.
# Without these, alerts are simulated: server log + on-screen banner + chime.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=whatsapp:+14155238886
